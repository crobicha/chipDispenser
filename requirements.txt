# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Hardware (only installs on real Pi)
# RPi.GPIO>=0.7.1  # Uncomment on Raspberry Pi

# Payment processors (add whichever you choose)
# stripe>=9.0.0
# squareup>=37.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# RFID (for future phase)
# mfrc522>=0.0.7
